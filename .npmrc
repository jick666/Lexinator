; ── reproducibility ─────────────────────────────────────────────
save-exact=true
lockfile-version=3
engine-strict=true

; ── CI tweaks ───────────────────────────────────────────────────
fund=false
update-notifier=false
progress=false
audit=true

; install *all* deps but never run lifecycle scripts
ignore-scripts=true     # keeps Husky quiet
;   ↓  ⬅︎ delete the old line
; only=prod