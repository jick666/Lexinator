; ────────────────────────────────────────────────────────────────
;  Lexinator – npm configuration
;  • proxy / https-proxy point to the values Codex injects
;    (HTTP_PROXY / HTTPS_PROXY).  No deprecated env-keys needed.
;  • save-exact + lockfile-version ensure repeatable builds.
;  • engine-strict makes “node >=18” in package.json a hard rule.
;  • fund/update-notifier/progress keep CI output clean.
; ────────────────────────────────────────────────────────────────

##### network / proxy ###########################################
proxy=${HTTP_PROXY}
https-proxy=${HTTPS_PROXY}

##### reproducibility ###########################################
save-exact=true          ; write exact versions, no ^ or ~
lockfile-version=3       ; always generate v3 package-lock.json
engine-strict=true       ; fail install if Node < 18 is used

##### CI / developer-experience tweaks ##########################
fund=false               ; hide “npm fund” banner
update-notifier=false    ; silence “new npm available” chatter
progress=false           ; slimmer logs in non-TTY CI
audit=true               ; keep “npm ci” failing on real vulns
ignore-scripts=false     ; fail if any lifecycle script errors

##### production installs #######################################
; When NODE_ENV=production only prod deps will be installed
only=prod
